/* #include <string>
#include <vector>
#include "Account.h"
#include "Librarian.h"
#include "Member.h"
#include "DevLibraries.h"

#define LibrarianN "Librarian"

class In
{
private:
    std::vector<std::string> list;
public:
    bool loadAccounts();
    static int Login(In in, std::string us, std::string pw, Account* temp);
    static bool Register(In &in, std::string us, std::string pw);
};


 */